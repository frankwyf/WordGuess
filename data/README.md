# Data Files

`words.txt` contains the word list used by the game.

`test.txt` is a smaller list used by unit tests.

Keep file names and relative paths unchanged unless you also update the code and tests.
